import { Link } from 'react-router-dom';
import Header from '../components/Header';

const Chat = () => {
  return (
    <Link to="/">
      <div>Log out</div>
    </Link>
  );
};

export default Chat;
